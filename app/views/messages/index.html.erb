<h1 id="temp_header">Chatting for pandas!</h1>

<ul id="chat">
		<%= render @messages %>
</ul>
<%= form_for Message.new, remote: true do |f| %>
	
	<%= f.text_field :content %>
	<%= f.submit 'Send'%>

<% end %>
<!-- <button id="applesh"></button>